import React from 'react'

const Loader = () => {
  return (
<div className="spinner container" style={{display : "flex", justifyContent : 'center', alignItems : "center"}} ></div>
  )
}

export default Loader