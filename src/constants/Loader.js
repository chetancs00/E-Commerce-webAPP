import React from 'react'

const Loader = () => {
  return (
<div class="spinner container" style={{display : "flex", justifyContent : 'center', alignItems : "center"}} ></div>
  )
}

export default Loader